import Bins from '../services/bins.js'

const Homepage = () => {
  // let data = await Bins.getAll()
  // console.log(data)

  return (
    <div>
      <h1>Bins</h1>
    </div>
  )
}

export default Homepage
